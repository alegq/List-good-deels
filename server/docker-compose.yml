version: '3'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    container_name: blog-api
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    container_name: blog-db
